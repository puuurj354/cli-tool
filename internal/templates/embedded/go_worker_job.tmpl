package job

import (
	"log"
	"time"
)

type Job struct {
	ID      int
	Payload string
}

func (j *Job) Process() error {
	// Simulate work
	time.Sleep(100 * time.Millisecond)
	log.Printf("Job %d completed: %s", j.ID, j.Payload)
	return nil
}
